import { createContext } from "react";

/**
 * @see https://es.reactjs.org/docs/context.html
 */
export default createContext(null);
// this context will be used to know if there are unregisteredUser credentials
